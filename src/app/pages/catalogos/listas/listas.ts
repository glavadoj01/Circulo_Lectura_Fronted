import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
    selector: 'app-listas',
    imports: [RouterLink],
    templateUrl: './listas.html',
})
export class Listas {}
